'use strict';

import SectionWhoWeAre from '../section_whoweare';

describe('SectionWhoWeAre View', function() {

  beforeEach(() => {
    this.sectionWhoWeAre = new SectionWhoWeAre();
  });

  it('Should run a few assertions', () => {
    expect(this.sectionWhoWeAre);
  });

});
