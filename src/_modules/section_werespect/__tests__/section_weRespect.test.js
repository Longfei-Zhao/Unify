'use strict';

import SectionWeRespect from '../section_werespect';

describe('SectionWeRespect View', function() {

  beforeEach(() => {
    this.sectionWeRespect = new SectionWeRespect();
  });

  it('Should run a few assertions', () => {
    expect(this.sectionWeRespect);
  });

});
