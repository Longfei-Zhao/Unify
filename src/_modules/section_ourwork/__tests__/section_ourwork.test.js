'use strict';

import SectionOurwork from '../section_ourwork';

describe('SectionOurwork View', function() {

  beforeEach(() => {
    this.sectionOurwork = new SectionOurwork();
  });

  it('Should run a few assertions', () => {
    expect(this.sectionOurwork);
  });

});
